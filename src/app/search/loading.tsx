import { ServerlessPageSkeleton } from '@/components/ui/ServerlessSkeleton';

export default function SearchLoading() {
  return <ServerlessPageSkeleton variant="search" label="Loading search results" />;
}
