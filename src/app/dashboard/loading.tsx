import { ServerlessPageSkeleton } from '@/components/ui/ServerlessSkeleton';

export default function DashboardLoading() {
  return <ServerlessPageSkeleton variant="dashboard" label="Loading dashboard" />;
}
