import { ServerlessPageSkeleton } from '@/components/ui/ServerlessSkeleton';

export default function ProfileLoading() {
  return <ServerlessPageSkeleton variant="profile" label="Loading profile" />;
}
